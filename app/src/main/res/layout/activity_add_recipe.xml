<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!--this field for the recipe's name -->
    <EditText
        android:id="@+id/editRecipeName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Recipe Name" />

    <!--this field for the recipe's instructions, allows multiple lines since instructions are usually long -->
    <EditText
        android:id="@+id/editRecipeInstructions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Instructions"
        android:inputType="textMultiLine"
        android:minLines="4" />

    <!--this saves the recipe and moves on to adding its ingredients -->
    <Button
        android:id="@+id/buttonSaveRecipe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Save Recipe and Add Ingredients" />

</LinearLayout>